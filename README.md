tivwp-sanitize-redirect
=======================

Do not remove non-ASCII characters from URLs on wp_redirect

To make sure, it works, place in the mu-plugins.
Otherwise, there is a chance that someone already loaded the original.
